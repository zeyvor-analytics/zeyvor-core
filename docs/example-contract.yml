# Zeyvor data contract — what these columns are supposed to mean.
#
# Generated by `zeyvor init`, then edited by hand and reviewed like code.
# `zeyvor check` compares live data against this file and fails the build when
# the data stops matching. Every clause is optional; omit one to stop checking
# it, or set `ignore: true` on a column to retire it while keeping the intent
# visible in review.

version: 1
generated_by: 'zeyvor 0.1.0  # descriptions unavailable: Error code: 401 - {''type'': ''error'', ''error'': {''type'': ''authentication_error'', ''message'': ''invalid x-api-key''}, ''request_id'': ''req_011CdeajjcuDK8YHmSuVGDcQ''}'
generated_at: '2026-08-02'
tables:
  clean_orders:
    source: /Users/mac/Documents/Zeyvor/zeyvor-core/tests/fixtures/clean_orders.csv
    profile_fingerprint: sha256:b37c6fb8e91a3fb9
    min_rows: 1
    columns:
      order_id:
        type: integer
        nullable: false
        unique: true
        no_pii: true
      customer_email:
        type: email
        nullable: false
      signup_date:
        type: date
        formats: ['####-##-##']
        nullable: false
        min: '2024-01-01'
        max: today
        no_pii: true
      status:
        type: text
        nullable: false
        categories: [delivered, pending, refunded, shipped]
        categories_closed: true
        no_pii: true
      amount:
        type: float
        nullable: false
        min: 0
        max: 800
        no_pii: true
      country:
        type: text
        formats: [aa]
        nullable: false
        categories: [DE, FR, GB, US]
        categories_closed: true
        no_pii: true
      item_count:
        type: integer
        nullable: false
        min: 0
        max: 20
        no_pii: true
